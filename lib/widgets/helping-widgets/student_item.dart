import 'package:flutter/material.dart';

class StudentDetailItems extends StatelessWidget {
  const StudentDetailItems({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}